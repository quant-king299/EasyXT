# 通达信预警信号对接QMT实盘交易指南（EasyXT集成版）

[配置说明](CONFIGURATION.md)

本方案展示了如何将EasyXT量化交易框架与通达信预警信号结合，实现自动化的程序交易。

## 📋 方案概述

通过集成tdxtrader和EasyXT，我们可以：

1. **读取通达信预警信号**：监控通达信生成的买卖信号
2. **使用EasyXT执行交易**：利用EasyXT封装的交易API执行买卖操作
3. **统一配置管理**：通过配置文件管理交易参数
4. **支持多种交易类型**：市价单、限价单等
5. **提供日志和通知**：交易日志记录和企业微信通知

本文档详细介绍如何将通达信预警信号与QMT实盘交易系统对接，实现自动化交易。通过本方案，您可以将通达信的技术指标预警信号自动转换为QMT的实盘交易指令。

## 🧠 工作原理

### 系统架构

```
通达信预警文件 → tdxtrader监听 → EasyXT API → QMT交易终端
```

### 核心组件

1. **通达信预警文件**：通达信软件生成的预警信号文件（sign.txt）
2. **tdxtrader模块**：监听预警文件变化并解析信号
3. **EasyXT API**：封装QMT交易接口，提供简化的交易API
4. **QMT交易终端**：迅投QMT实盘交易系统

## 📁 项目结构

```
strategies/tdxtrader/
├── easyxt_tdx_integration.py    # EasyXT与通达信集成主模块
├── tdxtrader_integration_example.py  # 集成示例
├── tdxtrader/                   # tdxtrader源码
│   ├── __init__.py
│   ├── index.py
│   ├── trader.py
│   ├── order.py
│   ├── file.py
│   ├── logger.py
│   ├── utils.py
│   └── anis.py
└── README_TDX_QMT_INTEGRATION.md # 本说明文件
```

## 🚀 快速开始

### 1. 环境准备

#### 系统要求
- Windows操作系统（QMT限制）
- Python 3.8+
- QMT交易终端（标准版或迷你版）
- 通达信金融终端

#### 安装依赖

```bash
# 安装项目依赖
pip install -r requirements.txt

# 确保xtquant库已正确部署
# 从项目Release中下载xtquant.rar并解压到项目根目录
```

### 2. 配置通达信预警

1. 在通达信中设置预警指标
2. 配置预警文件输出路径（如：`D:\new_tdx\sign.txt`）
3. 确保通达信正在运行并生成预警信号

#### 设置预警指标
1. 在通达信中创建技术指标公式
2. 设置预警条件（如KDJ金叉、MACD死叉等）
3. 配置预警输出格式

#### 预警文件路径
默认预警文件路径为：`D:/new_tdx/sign.txt`
可根据需要在配置文件中修改。

### 3. 配置交易参数

运行程序后会自动生成配置文件模板 `tdx_easyxt_config.json`：

```json
{
    "tdx_file_path": "D:/new_tdx/sign.txt",
    "interval": 1,
    "buy_signals": ["KDJ买入条件选股", "MACD买入条件选股"],
    "sell_signals": ["KDJ卖出条件选股", "MACD卖出条件选股"],
    "cancel_after": 10,
    "wechat_webhook_url": null,
    "default_volume": 100,
    "price_type": "limit"
}
```

#### 配置文件说明
配置文件位于：`strategies/tdxtrader/config/tdx_easyxt_config.json`

```json
{
    "tdx_file_path": "D:/new_tdx/sign.txt",     // 通达信预警文件路径
    "interval": 1,                              // 轮询间隔（秒）
    "buy_signals": [                           // 买入信号名称列表
        "KDJ买入条件选股",
        "MACD买入条件选股"
    ],
    "sell_signals": [                          // 卖出信号名称列表
        "KDJ卖出条件选股",
        "MACD卖出条件选股"
    ],
    "cancel_after": 10,                        // 未成交撤单时间（秒）
    "wechat_webhook_url": null,                // 企业微信机器人URL（可选）
    "default_volume": 100,                     // 默认交易数量
    "price_type": "市价"                       // 价格类型（市价/限价）
}
```

> **注意**：账户ID和QMT路径配置已移至项目根目录的统一配置文件中，请参考 [配置说明](CONFIGURATION.md) 进行配置。

#### 统一配置文件
账户信息和QMT路径配置在项目根目录的统一配置文件中：
`config/unified_config.json`

```json
{
  "settings": {
    "account": {
      "qmt_path": "D:\\国金QMT交易端模拟\\userdata_mini",  // QMT路径
      "account_id": "39020958"                             // 账户ID
    }
  }
}
```

### 4. 启动交易系统

```python
from strategies.tdxtrader.easyxt_tdx_integration import TDXEasyXTIntegration

# 初始化集成器
integration = TDXEasyXTIntegration("tdx_easyxt_config.json")

# 启动交易系统
integration.start_trading()
```

#### 使用示例
```python
# 创建集成器实例
integration = TDXEasyXTIntegration("my_config.json")

# 启动交易系统
integration.start_trading()
```

#### 命令行启动
```bash
cd c:\Users\Administrator\Desktop\miniqmt扩展
python strategies/tdxtrader/tdxtrader_integration_example.py
```

## 🔧 核心功能

### 1. 信号处理

- **买入信号处理**：当通达信生成买入信号时，自动执行买入操作
- **卖出信号处理**：当通达信生成卖出信号时，自动执行卖出操作
- **多信号支持**：支持多个买入/卖出信号

### 2. 交易执行

- **市价单/限价单**：支持不同类型的订单
- **自动计算数量**：根据配置自动计算交易数量
- **持仓检查**：卖出前检查是否有持仓
- **异常处理**：完善的异常处理机制

### 3. 配置管理

- **统一配置**：所有参数通过配置文件管理
- **默认值**：提供合理的默认配置
- **灵活扩展**：支持自定义配置项

## ⚙️ 配置说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| account_id | QMT账户ID | 从统一配置文件读取 |
| qmt_path | QMT用户数据路径 | 从统一配置文件读取 |
| tdx_file_path | 通达信预警文件路径 | "D:/new_tdx/sign.txt" |
| interval | 轮询间隔(秒) | 1 |
| buy_signals | 买入信号列表 | ["KDJ买入条件选股"] |
| sell_signals | 卖出信号列表 | ["KDJ卖出条件选股"] |
| cancel_after | 未成交撤单时间(秒) | 10 |
| wechat_webhook_url | 企业微信机器人URL | None |
| default_volume | 默认交易数量 | 100 |
| price_type | 价格类型(limit/market) | "limit" |

## 🔧 高级配置

### 1. 多信号处理
支持同时监听多个买入/卖出信号：
```json
{
    "buy_signals": [
        "KDJ买入条件选股",
        "MACD买入条件选股",
        "RSI超卖买入"
    ],
    "sell_signals": [
        "KDJ卖出条件选股",
        "MACD卖出条件选股",
        "RSI超买卖出"
    ]
}
```

### 2. 企业微信通知
配置企业微信机器人URL，实时接收交易通知：
```json
{
    "wechat_webhook_url": "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key"
}
```

### 3. 条件化交易
根据持仓、资产等条件决定是否交易：
```python
def buy_event(params):
    stock = params.get('stock')
    position = params.get('position')
    xt_trader = params.get('xt_trader')
    account = params.get('account')
    
    # 获取账户资产
    asset = xt_trader.query_stock_asset(account)
    
    # 只在总资产小于100万时买入
    if asset.total_asset < 1000000:
        return {
            'size': 100,
            'price': -1,
            'type': '市价'
        }
    else:
        return None  # 不执行交易
```

## 🛡️ 安全建议

1. **模拟盘测试**：首次使用前请在模拟盘充分测试
2. **参数验证**：确保配置参数正确无误
3. **风险控制**：设置合理的交易数量和金额
4. **日志监控**：定期检查交易日志
5. **网络稳定**：确保网络连接稳定

## 📊 监控与日志

### 日志级别
- **INFO**: 关键操作信息（如读取信号、下单成功等）
- **ERROR**: 错误信息（如下单失败、连接异常等）
- **DEBUG**: 详细调试信息（开发时使用）

### 日志文件
日志文件保存在：`strategies/tdxtrader/logs/`

## 📈 扩展功能

### 1. 自定义交易逻辑

可以重写 `buy_event` 和 `sell_event` 方法实现自定义交易逻辑：

```python
def custom_buy_event(self, params):
    # 自定义买入逻辑
    stock = params.get('stock')
    position = params.get('position')
    
    # 例如：根据持仓情况决定是否买入
    if position is None:
        # 无持仓时买入
        return {'size': 100, 'price': stock.get('price'), 'type': '限价'}
    else:
        # 有持仓时不买入
        return None
```

### 2. 集成其他通知方式

除了企业微信，还可以集成其他通知方式：

```python
# 邮件通知
def send_email_notification(subject, content):
    # 实现邮件发送逻辑
    pass

# 短信通知
def send_sms_notification(content):
    # 实现短信发送逻辑
    pass
```

## 🤝 集成优势

1. **简化API调用**：EasyXT封装了复杂的xtquant接口
2. **统一错误处理**：提供一致的错误处理机制
3. **配置化管理**：通过配置文件管理所有参数
4. **日志记录**：完整的交易日志记录
5. **易于扩展**：模块化设计，便于功能扩展

## 📊 使用示例

实现交易逻辑：

```python
def buy_event(params):
    """
    买入事件处理函数
    
    Args:
        params: 包含交易相关信息的字典
            - stock: 股票信息
            - position: 持仓信息
            - xt_trader: 交易对象
            - account: 账户对象
    """
    stock = params.get('stock')
    position = params.get('position')
    
    print(f"📈 买入信号触发: {stock.get('name')} ({stock.get('code')})")
    
    # 返回交易参数
    return {
        'size': 100,           # 交易数量
        'price': -1,           // 市价单使用-1
        'type': '市价'         // 价格类型
    }
```

```python
def sell_event(params):
    """
    卖出事件处理函数
    
    Args:
        params: 包含交易相关信息的字典
    """
    stock = params.get('stock')
    position = params.get('position')
    
    print(f"📉 卖出信号触发: {stock.get('name')} ({stock.get('code')})")
    
    # 检查持仓
    if position is None:
        print("⚠️  无持仓，不执行卖出操作")
        return None
    
    # 返回交易参数
    return {
        'size': position.can_use_volume,  // 卖出可用持仓
        'price': -1,                      // 市价单使用-1
        'type': '市价'                    // 价格类型
    }
```

## 🆘 故障排除

### 常见问题

1. **无法连接QMT**
   - 检查QMT是否已启动并登录
   - 确认账户ID和QMT路径配置正确
   - 检查QMT版本是否兼容

2. **无法读取预警文件**
   - 确认通达信预警已正确设置
   - 检查预警文件路径配置
   - 确保文件权限正确

3. **下单失败**
   - 检查交易时间（工作日9:30-15:00）
   - 确认账户资金充足
   - 检查股票是否停牌

### 调试方法

1. **启用详细日志**
   ```python
   # 在tdxtrader/logger.py中调整日志级别
   logger.setLevel(logging.DEBUG)
   ```

2. **手动测试交易**
   ```python
   from easy_xt import get_api
   
   easy_xt = get_api()
   # 测试连接
   if easy_xt.init_trade("D:\\国金QMT交易端模拟\\userdata_mini"):
       # 测试下单
       order_id = easy_xt.buy("39020958", "600000.SH", 100, 0, "市价")
       print(f"订单号: {order_id}")
   ```

## 📈 最佳实践

### 1. 风险控制
- 设置单票最大持仓比例
- 控制每日交易次数
- 实施止损止盈策略

### 2. 性能优化
- 合理设置轮询间隔
- 避免频繁查询账户信息
- 使用异步处理提高响应速度

### 3. 监控告警
- 定期检查日志文件
- 设置异常交易告警
- 监控账户资金变化

## 📚 相关文档

- [EasyXT使用指南](strategies/jq2qmt/qka/使用指南.md)
- [QMT交易接口文档](xtquant/doc/xttrader.md)
- [统一配置文件说明](config/unified_config.json)

## 📞 技术支持

如有问题，请联系：

- 查看EasyXT项目文档
- 提交GitHub Issue
- 加入交流群讨论

---

**免责声明**：本集成方案仅供学习和研究使用，不构成投资建议。使用本方案进行实际交易的风险由用户自行承担。